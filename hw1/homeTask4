numbers = []
for i in range(1, 11):
    numbers.append(i)
print(numbers)

str_numbers = [str(num) for num in numbers]
joined_numbers = ", ".join(str_numbers)
result = "Created in loop numbers: " + joined_numbers
print(result)

